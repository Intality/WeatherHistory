<Window x:Class="WeatherHistory.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Developer Coordinates"
        Height="200" Width="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2b2f36">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Latitude:" Foreground="White" />
        <TextBox x:Name="LatInput"
                 Grid.Row="1"
                 Margin="0,4,0,10" />

        <TextBlock Text="Longitude:" Grid.Row="2" Foreground="White" />
        <TextBox x:Name="LonInput"
                 Grid.Row="3"
                 Margin="0,4,0,10" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button Content="Cancel"
                    Width="70"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>

            <Button Content="Save"
                    Width="70"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
